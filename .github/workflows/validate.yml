name: validate

# This repo owns no CI logic of its own. It calls the reusable workflow owned by
# `dEitY719/harness-skills` (dotfiles #1410 D-10), so a new check lands here the
# moment it lands there — no copy to keep in sync.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: dEitY719/harness-skills/.github/workflows/skill-check.yml@main
    with:
      plugin-name: session
